{% extends 'base.jinja' %}

{% from '_helpers.jinja' import render_field %}

{% block main %}
<div class="container change_password">
  <div class="row account-auth">
    <div class="col-md-10 col-md-offset-1">
      <h4 class="center"><span class="reset_heading">{{ _('Enter Your New Password') }}</span></h4>
    </div>
    <div class="col-md-4 col-md-offset-4">
      <form class="form-horizontal" method="POST" autocomplete="off">
        {{ change_password_form.hidden_tag() }}
        {{ render_field(change_password_form.old_password, placeholder=_('Old Password'), required=required) }}
        {{ render_field(change_password_form.password, placeholder=_('Password'), required=required) }}
        {{ render_field(change_password_form.confirm, placeholder=_('Confirm Password'), required=required) }}
        <div class="form-group">
          <button type="submit" class="btn btn-lg btn-block">{{ _('Change Password') }}</button>
        </div>
      </form>
    </div>
  </div>
</div>
{% endblock main %}